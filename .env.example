# Database
DB_URL=jdbc:postgresql://localhost:5432/manajemen_risiko
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# Application and authentication
SERVER_PORT=8080
# Auth Service validates browser sessions at GET /user-info.
AUTH_API_URL=https://api-mahulu.kertaskerja.cc
MANRISK_AUTH_CONNECT_TIMEOUT_SECONDS=3
MANRISK_AUTH_REQUEST_TIMEOUT_SECONDS=3
# Transitional fallback for the legacy Next.js BFF only. Never expose this to browsers.
RISIKO_INTERNAL_TOKEN=replace_with_a_long_random_shared_token
SWAGGER_SERVER_URL=http://localhost:8080/manrisk/api

# Generate AI (OpenRouter)
# Keep disabled until OPENROUTER_API_KEY is set.
MANRISK_AI_ENABLED=false
# RISIKO_INTERNAL_TOKEN must exactly match the Next.js frontend server environment.
OPENROUTER_API_KEY=replace_with_your_openrouter_api_key
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_MODEL=openai/gpt-4.1-mini

# Optional safeguards; the defaults below match application.yml.
MANRISK_AI_RATE_LIMIT_PER_MINUTE=5
MANRISK_AI_MAX_CONCURRENT_REQUESTS=5
MANRISK_AI_CONNECT_TIMEOUT_SECONDS=3
MANRISK_AI_REQUEST_TIMEOUT_SECONDS=45
MANRISK_AI_PROVIDER_TIMEOUT_SECONDS=30
